let zero_float32 : float = 0.0 

let add_float32 (x: float) (y: float) : float = (x +. y)

let sub_float32 (x: float) (y: float) : float = (x -. y)

let mul_float32 (x: float) (y: float) : float = (x *. y)

let div_float32 (x: float) (y: float) : float = (x /. y)

let eq_float32 (x: float) (y: float) : bool = (x = y)

let lt_float32 (x: float) (y: float) : bool = (x < y)

let le_float32 (x: float) (y: float) : bool = (x <= y)

let gt_float32 (x: float) (y: float) : bool = (x > y)

let ge_float32 (x: float) (y: float) : bool = (x >= y)

type scalar_f32 = float

